version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
  # environment:
  #   - MONGODB_URI=your-atlas-uri-here
    volumes:
      - .:/app
  # networks:
  #   - app-network

# networks:
#   app-network:
#     driver: bridge